// note here we use === instead of == becoz if we use duble equal
if("2"==2){
    console.log(true)
}//here it returns true because it converts strings to number then compare but if use === then it not convert and match data type also
if("2"===2){
     console.log(true)
}
else{
    console.log(false)}
console.log(null>=0)   
console.log(null==0)   
console.log(null<=0)   
//so in java script always do comparision with same data type otherwise will give unsatisfied result